A minimal example using TouchId for local authentication, with fallback to a password stored in the keychain.

Uses [A0SimpleKeychain](https://github.com/auth0/SimpleKeychain) to access to the keychain.